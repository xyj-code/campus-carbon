server:
  port: 8080

spring:
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss
  datasource:
    url: jdbc:mysql://localhost:3306/campus_carbon?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root  # 将${your_mysql_username}替换为你的MySQL用户名
    password: 123456  # 将${your_mysql_password}替换为你的MySQL密码
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  type-aliases-package: com.campus.carbon.model
  configuration:
    map-underscore-to-camel-case: true

carbon:
  ai:
    api-key: sk-68a7a42f39454217bbddf1514c5fd7c9
    endpoint: https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions